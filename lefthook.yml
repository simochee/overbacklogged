pre-commit:
  parallel: true
  jobs:
    - name: oxlint
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx oxlint {staged_files}

    - name: oxfmt
      run: bunx oxfmt --check --no-error-on-unmatched-pattern {staged_files}
